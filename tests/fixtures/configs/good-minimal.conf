global {
    from   test@example.com
    debug  0
    trace  0
}

contacts {
    user1 {
        groups  admin
        email   user1@example.com
    }
}

rules {
    catch-all {
        pattern  {default}
        subject  {$log(host) event}
        delay    0
        email    admin
    }
}
